export type GraphDataType = {
  label: string;
  value: number;
};

export interface TableSummaryType {
  name: string;
  row_count: number;
  col_count: number;
  created: string;
  updated: string;
};
